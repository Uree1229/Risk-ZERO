#pragma once

// 이 파일을 config.h로 복사한 뒤 실제 값을 입력합니다.
// config.h는 Git에 올라가지 않습니다.
#define RISK_ZERO_WIFI_SSID "YOUR_WIFI_SSID"
#define RISK_ZERO_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define RISK_ZERO_DEVICE_ID "ESP32-CAM-01"

